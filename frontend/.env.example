# Base URL of the VeronzoApi backend (ASP.NET Core). Copy this file to .env.local
# and adjust for your environment — .env.local is gitignored, never commit real values.
VITE_API_URL=http://localhost:5103
